myproject/
├── Cargo.toml          # Workspace全局配置（统一依赖版本）
├── Makefile            # Linux快捷命令（如启动/构建）
├── build.bat           # Windows快捷命令
├── .gitignore          # Git忽略规则
├── README.md           # 项目说明文档
├── config/             # 环境配置目录（按模块隔离）
│   ├── common.toml     # 通用层配置
│   ├── social-service.toml  # 社交模块配置
│   ├── finance-service.toml # 金融模块配置
│   ├── erp-service.toml     # ERP模块配置
│   ├── mall-service.toml    # 商城模块配置
│   ├── blockchain-core.toml # 区块链公链配置
│   ├── blockchain-game.toml # 区块链游戏配置
│   └── website-service.toml  # 官网模块配置
├── deploy/             # 部署配置目录（按环境隔离）
│   ├── dev/            # 开发环境
│   ├── test/           # 测试环境
│   └── prod/           # 生产环境
│       └── website-service.yml # 官网部署配置
├── .github/workflows/  # GitHub Actions CI/CD配置
│   ├── ci.yml          # 代码检查/编译/测试
│   └── cd.yml          # 打包/部署
├── migrations/         # 数据库迁移文件（按模块隔离）
│   └── website-service/       # 官网数据库迁移（如新闻/表单记录）
│       └── 0001_init_website.sql
├── src/                # 根启动入口
│   └── main.rs         # 统一启动所有业务模块
└── crates/             # 核心业务模块（Workspace子包）
    ├── common/         # 通用能力层（所有模块复用）
    │   ├── src/
    │   │   ├── blockchain/ # 区块链通用能力
    │   │   ├── auth.rs     # 统一认证
    │   │   ├── db.rs       # 统一数据库连接
    │   │   └── log.rs      # 统一日志
    ├── social-service/ # 社交模块（Actix-web）
    ├── finance-service/ # 金融模块（Actix-web）
    ├── erp-service/    # ERP模块（Actix-web）
    ├── mall-service/   # 商城模块（Actix-web）
    ├── blockchain-core/ # 区块链公链核心
    ├── blockchain-game/ # 区块链游戏业务
    └── website-service/       # 官网核心模块（Actix-web + Dioxus）
        ├── static/            # 官网静态文件（HTML/CSS/JS/图片）
        │   ├── css/
        │   ├── js/
        │   ├── images/
        │   └── index.html
        ├── templates/         # 模板文件（可选）
        │   └── index.html
        ├── src/
        │   ├── lib.rs         # 模块入口
        │   ├── static.rs      # 静态文件服务
        │   ├── form.rs        # 联系表单提交接口
        │   └── news.rs        # 新闻公告接口
        ├── Cargo.toml         # 模块依赖配置
        └── Dockerfile         # 部署镜像配置